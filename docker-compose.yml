services:
  custom-quiz-host:
    image: ghcr.io/michamican/custom-quiz-host:latest
    ports:
      - "8080:8080"
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
